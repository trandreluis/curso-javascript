var emails = [];

emails[0] = 'joaoa@email.com';

console.log(emails[0]);

var nomes = ['João', 'Maria'];

console.log(nomes);

console.log('Tamanho:', nomes.length);

nomes[4] = 'José';

console.log('Quantos nomes depois do José?', nomes.length);

// Espaços vazios
console.log(nomes);